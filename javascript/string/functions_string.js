let data;

data.length; //length retorna a quantidade de caracteres

data.toUpperCase(); //- transforma todas as letras em maiúsculas
data.toLowerCase(); //- transforma todas as letras em minusculas

data.trim(); //- tira espaços desnecessários

data.padStart(nº, ''); //- preenche as strings n vezes com o valor espaço vazio nesse caso.
data.padEnd(nº, '.'); //- preenche as strings n vezes com o valor ponto nesse caso.

data.replace(um, outro); //- troca um termo pelo outro

data.substg(inicio, quantidade_de_letras); //- extrair uma substring de uma string maior
data.substring(inicio, fim); //- extrair uma substring de uma string maior
